const {
    logger,
    requestCorrelationLogger,
    requestLogger,
    sanitizePayload,
    formatLogEntry,
    getRequestMetrics
} = require('../utils/logger');

module.exports = {
    logger,
    requestCorrelationLogger,
    requestLogger,
    sanitizePayload,
    formatLogEntry,
    getRequestMetrics
};
